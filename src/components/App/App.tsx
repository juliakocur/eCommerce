import { HashRouter } from 'react-router-dom';
import BasePage from '../BasePage/BasePage';

const App = () => {
  return (
    <HashRouter>
      <BasePage />
    </HashRouter>
  );
};

export default App;
